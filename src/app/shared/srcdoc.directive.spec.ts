import { SrcdocDirective } from './srcdoc.directive';

describe('SrcdocDirective', () => {
  it('should create an instance', () => {
    const directive = new SrcdocDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
